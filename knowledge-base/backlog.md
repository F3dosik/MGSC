# Backlog

Список открытых задач по курсовой. Обновляется по мере прохождения. Формат:

```
- [ ] Заголовок задачи — краткий contex/why
      Acceptance: что считаем выполнением.
```

Закрытые задачи переносятся в [[log]] как ingest/meta-записи, а из бэклога удаляются.

---

## Метаорганизация

- [ ] Зафиксировать даты дедлайнов в этом файле и в `project-deadlines` памяти LLM
      Acceptance: даты добавлены, я учитываю их при планировании.

## База знаний — приоритет 1 (нужно для генератора)

- [x] Ingest **Clark-Jacob-Stepney 2005** — специфика SA для S-блоков
      ✓ Страница [[sa-for-sboxes]] создана (2026-05-17). Включает: мутацию swap, CJS cost, двухфазный SA+HC, результаты литературы, причину стены NL=100→112.
- [ ] Ingest источника по **бент-функциям** (Токарева 2011)
      Acceptance: страница `wiki/entities/bent-functions.md` — что это, теорема о существовании только при чётном $n$, потолок $N = 2^{n-1} - 2^{n/2-1}$, несбалансированность → почему недостижимо для биективных S-блоков.

## База знаний — приоритет 2 (полная теория для отчёта; код уже работает)

- [ ] Ingest **higher-order differential attack** (Lai 1994, Knudsen 1995)
      Acceptance: расширить [[higher-order-differential-attack]] из стаба — определение производной порядка $k$, доказательство «$\deg f = d \Rightarrow$ производная порядка $d+1$ зануляется», обзор атаки, защита через $\deg \geq 7$. Источники в библиографии Heys 2002 ([21]).
- [ ] Ingest **алгебраической атаки** (Courtois-Meier 2003; Meier-Pasalic-Carlet 2004)
      Acceptance: расширить [[algebraic-attack]] и [[algebraic-immunity]] историей вопроса, доказательством границы $AI \leq \lceil n/2 \rceil$, описанием атаки на потоковые шифры через аннигиляторы. Код уже есть (`boolfunc/metrics.go:122`).
- [ ] Ingest **APN-функций и нижних границ $\delta$** (Nyberg 1993, Carlet гл. 11)
      Acceptance: расширить [[differential-uniformity]] концепцией APN ($\delta = 2$), теоремами о минимальных $\delta$ для биективных $n=2k$ (нижняя граница 4 для чётных $n$, открытая проблема existence биективных APN для $n=8$). Код уже есть (`sbox/ddt.go`).
- [ ] Ingest **Carlet 2021** (Boolean Functions for Cryptography and Coding Theory) — стандартная монография, ссылка в библиографии обеих курсовых
      Acceptance: страница `wiki/entities/carlet-2021-reference.md` со ссылками на главы; обновить страницы метрик с цитатами.

## Код — параллелизация (для 16-ядерной машины)

- [ ] (Advanced) Parallel Tempering
      Acceptance: $N$ SA с разными $T$, периодический обмен состояниями по Metropolis. Принципиально другой алгоритм; не блокирует multi-start. Источники: Earl & Deem 2005.

## Код — приоритет 1 (генератор)

- [x] Реализовать `generate/annealing.go` — simulated annealing с настраиваемым расписанием
      ✓ Реализовано (2026-05-15). SA, EstimateT0, ParallelMultiStartSA, OnTick callback для диагностики.
- [x] Реализовать базовые cost-функции в `generate/cost.go`
      ✓ ClarkJacobStepneyNL, MinMaxWalsh, ThresholdNL реализованы и проверены экспериментами (2026-05-17).

- [ ] **[ИССЛЕДОВАНИЕ ЗАВЕРШЕНО]** SA-генератор: серия E-01..E-10
      Итог (2026-05-21): два изолированных бассейна — NL=100 (из random, стабильно) и NL=112 (только из AES, воспроизводит AES). Переход между ними через swap-мутации не достигается. Результат зафиксирован в [[sa-experiments]].

- [ ] `generate/generator.go` — общий интерфейс генератора + случайная биективная подстановка (Fisher-Yates) как baseline
      Acceptance: интерфейс `Generator { Generate() *sbox.SBox }`; baseline `RandomGenerator` проходит тест на биективность.
- [ ] Реализовать полную **fitness-функцию** из тезисов (все 4 метрики)
      Контекст: MinMaxWalsh и ThresholdNL только для NL. Нужна `ThresholdFull(w1,w2,w3,w4)` по формуле из [[fitness-function-sbox]].
      Acceptance: для AES — cost=0; для random — высокое значение; покрыта тестом.

## Код — приоритет 3 (мелкие находки из ревью)

- [ ] `BoolFunc.BestAffineApproximation()` — поле `bestAff []uint8` в структуре есть (`boolfunc/boolfunc.go:25`), публичного метода нет.
      Acceptance: метод возвращает TV ближайшей аффинной функции; обновить секцию «Реализация» в [[nonlinearity]].
- [ ] Публичный API для производной по направлению: `BoolFunc.Derivative(a int)`, `SBox.Derivative(a uint8)`.
      Acceptance: вычисляет $f'_a$ или $S'_a$; обновить [[directional-derivative]].
- [ ] Реализовать LAT и `SBox.Linearity()`
      Acceptance: построение LAT через [[walsh-hadamard|ПУА]] компонент (см. формулу $LAT = \tfrac{1}{2}\hat{f}_\beta$ в [[linear-approximation-table]]); метрика — максимум модуля. Также вернуть распределение нелинейностей координатных функций ($\overline{N}$, $N_{\min}$) — см. [[sbox-as-boolean]].

## Эксперименты — приоритет 1 («мясо» курсовой)

- [ ] Эксперимент: SA, $N$ запусков, чувствительность к параметрам (температура, скорость остывания)
      Acceptance: таблица в `wiki/entities/sa-experiments.md`; выводы о лучших параметрах.
      Контекст (2026-05-22): частично выполнено через E-04..E-09c — есть данные по разным $\alpha$, $M$, $T_{\min}$. Можно либо считать закрытым и убрать, либо дооформить сравнительной таблицей.

## Текст работы — приоритет 1 (СЕЙЧАС)

- [ ] **[СЛЕДУЮЩИЙ ШАГ]** Написать ЗАКЛЮЧЕНИЕ работы
      Контекст: гл. 7 готова черновиком (2026-05-22). Заключение должно сводить три тезиса гл. 7 — корректность инструмента; структурный потолок SA = NL=100 и два изолированных бассейна; неприменимость CJS для биективных 8×8 — на уровень всей работы, обозначив достигнутый практический результат (профиль уровня «Кузнечика»/GOST R 34.12-2015) и направления дальнейшей работы (cost-функция со спектральным разделением точек NL=100 и NL≥104; нелокальные мутации; алгебраические построения).
      Acceptance: раздел ЗАКЛЮЧЕНИЕ в `report/content.tex` объёмом ~1.5–2 стр., согласованный со стилем глав 5–7 (без AI-маркеров, конкретные числа, личный голос).

## Текст работы — приоритет 3 (под конец)

- [ ] Структура отчёта (введение, теория, методы, результаты, выводы) — наброски в `content.tex` уже есть, расширить.
- [ ] Согласовать с научруком в университете (если есть требования к оформлению).
